Metadata: 
  License: Apache-2.0
AWSTemplateFormatVersion: '2010-09-09'
Description: 'AWS CloudFormation Template to create an GPU based EC2 instance with NICE DCV pre installed.'
Parameters:
  InstanceType:
    Description: Choose the instance type from the dropdown.
    Type: String
    ConstraintDescription: must be a valid EC2 instance type.
  EBSVolumeSize:
    Description: The initial size of the volume (in GBs)  EBS will use for storage.
    Type: Number
    Default: 70  
  Namespace:
    Type: String
    Description: An environment name that will be prefixed to resource names
  S3Mounts:
    Type: String
    Description: A JSON array of objects with name, bucket, and prefix properties used to mount data
  IamPolicyDocument:
    Type: String
    Description: The IAM policy to be associated with the launched workstation
  EnvironmentInstanceFiles:
    Type: String
    Description: >-
      An S3 URI (starting with "s3://") that specifies the location of files to be copied to
      the environment instance, including any bootstrap scripts
  
  # AvailabilityZone:
  #   Description: Select the availability zone in which to create the instance. If you plan to attach a secondary volume to the instance, create this instance in the same AvailabilityZone as the volume you created.
  #   Type: AWS::EC2::AvailabilityZone::Name    
  # Password:
  #   Type: String
  #   NoEcho: True
  #   Description: Password for the default ec2-user account. Must be at least 8 characters long.
  #   MinLength: 8
  #   ConstraintDescription: Password must be at least 8 characters long.

Conditions:
  IamPolicyEmpty: !Equals [!Ref IamPolicyDocument, '{}']

Resources:  
  SSMPolicy:
    Type: AWS::IAM::ManagedPolicy
    Properties:
      ManagedPolicyName: !Join ["-", [!Ref Namespace, "SSM-Policy"]]
      PolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Sid: AllowSSMParameterAccess
            Effect: Allow
            Action:
              - ssm:PutParameter
              - ssm:GetParameter
              - ssm:GetParameters
              - ssm:DescribeParameters
            Resource: "*"
          - Sid: AllowKMSDecrypt
            Effect: Allow
            Action:
              - kms:Decrypt
              - kms:Encrypt
              - kms:GenerateDataKey
              - kms:DescribeKey
            Resource: "*"
  InstanceRolePermissionBoundary:
    Type: AWS::IAM::ManagedPolicy
    Properties:
      Description: Permission boundary for EC2 instance role
      ManagedPolicyName: !Join ['-', [Ref: Namespace, 'ec2-linux-permission-boundary']]
      PolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Action:
              - 's3:*'
            Resource: '*'
          - Effect: Allow
            Action:
              - 's3files:*'
            Resource: '*'
          - Effect: Allow
            Action:
              - 'sts:*'
            Resource: '*'          
          - Effect: Allow
            Action:
              - 'kms:*'
            Resource: '*'
          - Effect: Allow
            Action:
              - 'ssm:*'
            Resource: '*'
          - Effect: Allow
            Action:
              - 'ssmmessages:*'
            Resource: '*'
          - Effect: Allow
            Action:
              - 'ec2messages:*'
            Resource: '*'
          - Effect: Allow
            Action:
              - 'sns:Publish'     
            Resource: '*'                    
          
  IAMRole:
    Type: 'AWS::IAM::Role'
    Properties:
      RoleName: !Join ['-', [Ref: Namespace, 'ec2-role']]
      Path: '/'
      AssumeRolePolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: 'Allow'
            Principal:
              Service:
                - 'ec2.amazonaws.com'
            Action:
              - 'sts:AssumeRole'
      Policies:
        - !If
          - IamPolicyEmpty
          - !Ref 'AWS::NoValue'
          - PolicyName: !Join ['-', [Ref: Namespace, 's3-studydata-policy']]
            PolicyDocument: !Ref IamPolicyDocument
      ManagedPolicyArns:
        - Ref: SSMPolicy
        - arn:aws:iam::aws:policy/AmazonSSMManagedInstanceCore
        - arn:aws:iam::aws:policy/AmazonS3FilesClientFullAccess
      PermissionsBoundary: !Ref InstanceRolePermissionBoundary



  InstanceProfile:
    Type: 'AWS::IAM::InstanceProfile'
    Properties:
      InstanceProfileName: !Join ['-', [Ref: Namespace, 'ec2-profile']]
      Path: '/'
      Roles:
        - Ref: IAMRole

  EC2Instance:
    Type: AWS::EC2::Instance
    CreationPolicy:
      ResourceSignal:
        Timeout: PT15M
    Metadata:
      AWS::CloudFormation::Init:
        configSets:
          default:
            - config1
        config1:
          files:
            "/home/ec2-user/.config/autostart/studymount.desktop":
              mode: "000644"
              owner: "ec2-user"
              group: "ec2-user"
              content: |
                [Desktop Entry]
                Type=Application
                Exec="/home/ec2-user/mount_study.sh"
                Hidden=false
                NoDisplay=false
                X-GNOME-Autostart-enabled=true
                Terminal=true
                Name=Study-Mounting
                Comment=Study Mounting
            "/home/ec2-user/rstudio.sh":
              mode: "000755"
              owner: "ec2-user"
              group: "ec2-user"
              content: !Sub |
                #!/bin/bash
                set -euo pipefail
                # Ensure 8787 is free before starting rstudio container.
                # This handles both stale containers and non-docker listeners.
                docker rm -f rstudio >/dev/null 2>&1 || true
                pids=$(ss -ltnp 'sport = :8787' 2>/dev/null | sed -n 's/.*pid=\([0-9]\+\).*/\1/p' | sort -u)
                if [ -n "$pids" ]; then
                  echo "$pids" | xargs -r kill -9 || true
                fi
                if [ -z "$(docker ps -f "name=rstudio" -f "status=running" -q)" ]; then
                  docker rm -f rstudio >/dev/null 2>&1 || true
                  docker run --rm -d --name rstudio -e DISABLE_AUTH=true -v /home/ec2-user:/home/ec2-user -p 8787:8787 relevancelab/rstudio_4.2.1:1.0.3
                  sleep 3
                fi
                xdg-open "http://localhost:8787/" >/dev/null 2>&1 || true
            "/home/ec2-user/jupyter.sh":
              mode: "000755"
              owner: "ec2-user"
              group: "ec2-user"
              content: !Sub |
                #!/bin/bash
                set -euo pipefail
                if [ -z "$(docker ps -f "name=jupyterlab" -f "status=running" -q)" ]; then
                  docker rm -f jupyterlab >/dev/null 2>&1 || true
                  docker run --rm -d --name jupyterlab -p 8888:8888 -v /home/ec2-user:/home/ec2-user relevancelab/jupiterlab_3.5.0:1.0.3 \
                    jupyter lab --ip=0.0.0.0 --port=8888 --no-browser --ServerApp.token='' --ServerApp.password=''
                  sleep 3
                fi
                xdg-open "http://127.0.0.1:8888/lab" >/dev/null 2>&1 || true
            "/etc/firefox/policies/policies.json":
              mode: "000644"
              owner: "root"
              group: "root"
              content: |
                {
                  "policies": {
                    "Preferences": {
                      "browser.topsites.useRemoteSetting": {
                        "Value": false,
                        "Status": "default"
                      },
                      "browser.newtabpage.pinned": {
                        "Value": "[{\"url\":\"http://127.0.0.1:8787/\",\"label\":\"RStudio\",\"searchTopSite\":false},{\"url\":\"http://127.0.0.1:8888/lab\",\"label\":\"JupyterLab\",\"searchTopSite\":false}]",
                        "Status": "default"
                      }
                    }
                  }
                }
            "/home/ec2-user/mount_study.sh":
              mode: "000755"
              owner: "ec2-user"
              group: "ec2-user"
              content: !Sub |
                #!/bin/sh
                set -x
                # Adding sleep to avoid mounting failure
                sleep 30
                mount_s3.sh 2>&1 >> /home/ec2-user/mount_s3.log
            "/home/ec2-user/set_user_token.sh":
              mode: "000755"
              owner: "ec2-user"
              group: "ec2-user"
              content: !Sub |
                #!/bin/bash
                trap '/opt/aws/bin/cfn-signal --exit-code 1 --resource EC2Instance --region ${AWS::Region} --stack ${AWS::StackName}' ERR

                # Get the session token
                TOKEN=$(curl -s -X PUT "http://169.254.169.254/latest/api/token" -H "X-aws-ec2-metadata-token-ttl-seconds: 21600")

                # Use instance metadata for region/instance so token is always written to the running instance region.
                instance_id=$(curl -s -H "X-aws-ec2-metadata-token: $TOKEN" http://169.254.169.254/latest/meta-data/instance-id)
                region=$(curl -s -H "X-aws-ec2-metadata-token: $TOKEN" http://169.254.169.254/latest/meta-data/placement/region)
                echo "Retrieved instance_id $instance_id from metadata service"
                session_id="rg-session"
                auth_token=$(uuidgen)
                parameter_name="/RL/RG/nice-dcv/auth-token/$instance_id"
                aws ssm put-parameter --name "$parameter_name" --type "String" --value '{"auth_token":"'$auth_token'","session_id":"'$session_id'"}' --region "$region" --overwrite
                echo "User token set successfully"
            "/etc/systemd/system/dcv-auth.service":
              mode: "000644"
              owner: "root"
              group: "root"
              content: !Sub |
                [Unit]
                Description=NICE DCV Auth Service
                After=network-online.target docker.service
                Wants=network-online.target
                Requires=docker.service

                [Service]
                TimeoutStartSec=0
                Restart=always
                RestartSec=5
                Environment=AWS_REGION=${AWS::Region}
                Environment=AWS_DEFAULT_REGION=${AWS::Region}

                ExecStartPre=/bin/sleep 10
                ExecStartPre=-/usr/bin/docker stop dcv-auth
                ExecStartPre=-/usr/bin/docker rm dcv-auth

                ExecStart=/usr/bin/docker run \
                  --name dcv-auth \
                  --restart unless-stopped \
                  -p 8445:8445 \
                  -e PORT=8445 \
                  -e PARAMNAMEPREFIX=/RL/RG/nice-dcv/auth-token/ \
                  -e AWS_REGION=${AWS::Region} \
                  -e AWS_DEFAULT_REGION=${AWS::Region} \
                  relevancelab/nice-dcv-auth-svc:1.0.2

                ExecStop=/usr/bin/docker stop dcv-auth

                [Install]
                WantedBy=multi-user.target
            "/home/ec2-user/disable_lock.sh":
              content: !Sub |
                #!/bin/bash
                gsettings set org.gnome.desktop.lockdown disable-lock-screen 'true'
                echo "Lock disabled successfully"
              mode: "000755"
              owner: "ec2-user"
              group: "ec2-user"
    Properties:
      UserData:
        Fn::Base64: !Sub |
          #!/usr/bin/env bash
          exec > >(tee /var/log/user-data.log|logger -t user-data -s 2>/dev/console) 2>&1
          # trap '/opt/aws/bin/cfn-signal --exit-code 1 --resource EC2Instance --region ${AWS::Region} --stack ${AWS::StackName}' ERR
          sudo dnf install -y amazon-efs-utils
          # Download and execute bootstrap script
          aws s3 cp --region "${AWS::Region}" "${EnvironmentInstanceFiles}/get_bootstrap.sh" "/tmp"
          chmod 500 "/tmp/get_bootstrap.sh"
          /tmp/get_bootstrap.sh "${EnvironmentInstanceFiles}" '${S3Mounts}' "" "${AWS::Region}"  

          # Install Codex CLI early (before DCV/Docker start)
          sudo -u ec2-user bash -lc 'curl -fsSL https://chatgpt.com/codex/install.sh | sh'
          [ -x /home/ec2-user/.local/bin/codex ] && ln -sf /home/ec2-user/.local/bin/codex /usr/local/bin/codex && (grep -q '.local/bin' /home/ec2-user/.bashrc || echo 'export PATH="$HOME/.local/bin:$PATH"' >> /home/ec2-user/.bashrc)

          # Create dcv session start script (GDM must stay off; it conflicts with DCV virtual sessions)
          cat << 'EOF' > /usr/local/bin/start-dcv-session
          #!/bin/bash
          systemctl stop gdm 2>/dev/null || true
          systemctl disable gdm 2>/dev/null || true
          dcv close-session lock-session 2>/dev/null || true

          service_name="dcvserver"
          timeout=60
          elapsed_time=0

          echo "Waiting for $service_name to become active..."
          while true; do
            status=$(systemctl is-active $service_name)
            if [[ $status == "active" ]]; then
              echo "$service_name is now active."
              break
            fi
            if [[ $elapsed_time -ge $timeout ]]; then
              echo "Timeout: $service_name did not become active within $timeout seconds."
              break
            fi
            sleep 1
            elapsed_time=$((elapsed_time+1))
          done

          dcv close-session rg-session 2>/dev/null || true
          dcv create-session rg-session --name rg-session --type=virtual --user ec2-user --owner ec2-user
          dcv list-sessions
          date
          EOF

          # Create dcv session on reboot
          # chown root: "/usr/local/bin/start-dcv-session"
          # chmod 775 "/usr/local/bin/start-dcv-session"
          sleep 5
          # Clean existing containers/ports so this block is safe on reruns
          docker stop rstudio jupyterlab 2>/dev/null || true
          docker rm -f rstudio jupyterlab 2>/dev/null || true

          # Firefox New Tab Top Sites: pin RStudio and JupyterLab (policy file written by cfn-init)
          mkdir -p /etc/firefox/policies

          # Run init script to create files
          /opt/aws/bin/cfn-init --verbose --stack ${AWS::StackName} --resource EC2Instance --region ${AWS::Region}


          # -----------------------------
          # CREATE TOKEN BEFORE AUTH START
          # -----------------------------

          echo "Creating DCV auth token..."
          sudo -u ec2-user /home/ec2-user/set_user_token.sh

          echo "Waiting for token propagation..."

          IMDS_TOKEN=$(curl -s -X PUT "http://169.254.169.254/latest/api/token" \
          -H "X-aws-ec2-metadata-token-ttl-seconds: 21600")

          INSTANCE_ID=$(curl -s -H "X-aws-ec2-metadata-token: $IMDS_TOKEN" \
          http://169.254.169.254/latest/meta-data/instance-id)

          REGION=$(curl -s -H "X-aws-ec2-metadata-token: $IMDS_TOKEN" \
          http://169.254.169.254/latest/meta-data/placement/region)

          for i in {1..15}
          do
            TOKEN_VALUE=$(aws ssm get-parameter \
              --name "/RL/RG/nice-dcv/auth-token/$INSTANCE_ID" \
              --region "$REGION" \
              --query Parameter.Value \
              --output text 2>/dev/null)

            if [ ! -z "$TOKEN_VALUE" ]; then
              echo "Token confirmed in SSM"
              break
            fi

            echo "Waiting..."
            sleep 2
          done

          # -----------------------------
          # NOW START AUTH SERVICE
          # -----------------------------

          systemctl daemon-reload
          systemctl enable dcv-auth
          systemctl restart dcv-auth

          echo "Restarting dcvserver..."
          systemctl restart dcvserver

          sleep 10

          # GDM auto-login conflicts with DCV virtual sessions (especially on CPU instance types)
          echo "Disabling GDM for DCV virtual sessions..."
          systemctl stop gdm 2>/dev/null || true
          systemctl disable gdm 2>/dev/null || true

          # Create dcv session on reboot
          chown root: "/usr/local/bin/start-dcv-session"
          chmod 775 "/usr/local/bin/start-dcv-session"
          sh "/usr/local/bin/start-dcv-session"

          # Add command to start on reboot
          crontab -l 2>/dev/null > /tmp/crontab
          echo '@reboot systemctl stop gdm' >> "/tmp/crontab"
          echo '@reboot systemctl start dcvserver' >> "/tmp/crontab"
          echo '@reboot /usr/local/bin/start-dcv-session 2>&1 >> /var/log/start-dcv-session.log' >> "/tmp/crontab"
          echo '@reboot /home/ec2-user/set_user_token.sh 2>&1 >> /var/log/set_user_token.log' >> "/tmp/crontab"
          echo '@reboot systemctl start dcv-auth' >> "/tmp/crontab"

          crontab "/tmp/crontab"

          passwd -l ec2-user

          sudo -u ec2-user /home/ec2-user/rstudio.sh
          sudo -u ec2-user /home/ec2-user/jupyter.sh
          dcv list-sessions

          echo "Session ready. Sending signal."
          /opt/aws/bin/cfn-signal -e $? --stack ${AWS::StackName} --resource EC2Instance --region ${AWS::Region}
      InstanceType: !Ref 'InstanceType'
      SecurityGroups: [!Ref 'InstanceSecurityGroup']
      ImageId: "{{resolve:ssm:/RL/RG/StandardCatalog/linux-nice-dcv-ami}}"
      IamInstanceProfile: !Ref InstanceProfile
      BlockDeviceMappings:
        - DeviceName: /dev/xvda
          Ebs:
            VolumeSize: !Ref EBSVolumeSize
            Encrypted: true
            
      

      PropagateTagsToVolumeOnCreation: true
      Tags:
        - Key: Name
          Value: !Join ['-', [Ref: Namespace, 'ec2-linux']]
        - Key: Description
          Value: EC2 workspace instance
        - Key: cost_resource
          Value: !Sub ${AWS::StackName}
    
  InstanceSecurityGroup:
    Type: AWS::EC2::SecurityGroup
    Properties:
      GroupDescription: Enable SSH access
      SecurityGroupIngress:
      - IpProtocol: tcp
        FromPort: '22'
        ToPort: '22'
        CidrIp: 0.0.0.0/0
      - IpProtocol: tcp
        FromPort: '8443'
        ToPort: '8443'
        CidrIp: 0.0.0.0/0

Outputs:
  InstanceId:
    Description: InstanceId of the newly created EC2 instance
    Value: !Ref 'EC2Instance'
  InstanceIPAddress:
    Description: IP address of the newly created EC2 instance
    Value: !GetAtt [EC2Instance, PublicIp]
  InstanceDNSName:
    Description: DNS name of the newly created EC2 instance
    Value: !GetAtt [EC2Instance, PublicDnsName]
  ApplicationPort:
    Description: The Port in which the application is running
    Value: '8443'
  AvailabilityZone: 
    Description: AvailabilityZone of newly created EC2 instance
    Value: !GetAtt [EC2Instance, AvailabilityZone]